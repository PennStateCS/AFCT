name: Publish docs

on:
  push:
    branches: ['main']
  workflow_dispatch:

# Least privilege plus what GitHub Pages deployment needs.
permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; newer pushes supersede queued ones.
concurrency:
  group: pages
  cancel-in-progress: true

# The ERD generator (not run here) pulls in puppeteer, whose install would
# otherwise download a ~150MB Chromium this job never uses.
env:
  PUPPETEER_SKIP_DOWNLOAD: '1'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            docs-site/package-lock.json

      # The generator only needs js-yaml; skip the Prisma client generation.
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
        env:
          SKIP_PRISMA_GENERATE: '1'

      - name: Build the docs site
        run: |
          cd docs-site
          npm ci
          npm run build

      # docs:api also validates the emitted spec against the OpenAPI schema and
      # fails on a malformed @openapi block, so a bad spec can't reach Pages.
      # Written straight into the site build so the whole thing ships as one
      # artifact: Docusaurus at /, the OpenAPI reference at /api/.
      - name: Generate and validate OpenAPI docs
        run: npm run docs:api
        env:
          DOCS_OUT: docs-site/build/api

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs-site/build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
